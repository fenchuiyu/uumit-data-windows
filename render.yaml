# Render.com 部署配置
services:
  - type: web
    name: uumit-data-windows
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server_http.py
    envVars:
      - key: DEMO_MODE
        value: "1"
      - key: PORT
        value: "8000"
